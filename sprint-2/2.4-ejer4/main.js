'use strict';

function countBox(a, b, c, d) {
    const borderBox = a;

    if (borderBox === a) {
        countBox = b;
    } else {
        countbox = b, c, d;
    }
    
}
const resultCountbox = countBox(true, 80, 20, 10);
console.log(resultCountbox);